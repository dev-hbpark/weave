# Context Loading Guide — weave

This file is project-anchored at `workspace/weave/`. All `docs/...`, `features/...`, `packages/...`, `apps/...`, `records/...` paths referenced from this project's CLAUDE.md / RULE.md / navigation files resolve against this project root only. The OS-root equivalent is `docs/00-navigation/WORKSPACE_CONTEXT_GUIDE.md`; do not load it during project-internal work.

Read narrowly. Prefer feature-local context.
