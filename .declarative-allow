# weave — Rule 6 (declarative branching) allowlist.
#
# Lines are regex patterns matched against `<file>:<lineno>`. Each entry
# documents a deliberate exception to the switch/mode-compare ban.
# Justify in a code comment + add it here. The gate is in
# `tools/check_declarative_dispatch.sh`.

# Documentation comment that QUOTES the historical 18-case switch in
# the file header — explanatory text, not a live branch. Removing the
# quote would lose the migration history (AUDIT-002 / WI-026).
# (Now also covered by the gate's comment-line skip; kept as a backstop.)
^apps/web/src/document/toolbar/ContextualToolbar\.tsx:11$

# ── AUDIT-005 permitted exceptions ──────────────────────────────────────────

# migrate-shape-to-line — a one-time load migration (DR-025 / WI-062 Phase 6)
# that rewrites the FROZEN legacy "line-as-shape" sub-kinds (poly / line / arrow)
# to the `line` kind. Pure transform over a closed, retired input set — no new
# legacy shapes will ever be added, so this is the "pure transform" permitted
# exception. Idempotent; deleted once no persisted legacy shapes remain.
^apps/web/src/document/migrate-shape-to-line\.ts:75$
^apps/web/src/document/migrate-shape-to-line\.ts:77$

# changeToPatch — single-site invariant sibling: the one place that maps every
# agocraft Change variant to its Patch shape for the sync mirror (same category
# as serializer.invertPatch). Documented inline at the call site. The Change
# union is the (de)serialization boundary, not registrable per-kind behavior.
^apps/web/src/document/use-weave-editor\.ts:449$

# ── AUDIT-007 — pure value-transforms over closed/frozen intrinsic unions ────
# Surfaced when AUDIT-007 widened the gate to catch bare `switch (mode)`. Both
# are single-site, side-effect-free value→value maps over a closed union with no
# extension/registration story — the "pure transform / single-case discriminator"
# permitted exception (same category as agocraft's paint/filter geometry switches).

# layoutChildFromTextAutoResize — maps the FROZEN legacy `LegacyTextAutoResize`
# mode (agocraft v10 removed `TextAttrs.textAutoResize`; this is the compat shim)
# to a canonical `LayoutChildPolicy`. Retired union — will never grow.
^apps/web/src/document/domains/derive-text-auto-resize\.ts:52$

# entranceKeyframes — maps the closed `EntranceAnimationBehavior["mode"]`
# (fade / slide-up / slide-down / zoom-in) to Web-Animations keyframes. Pure
# render transform over a single behavior's own intrinsic animation vocabulary.
^apps/web/src/pages/PresentPage\.tsx:27$
